# FastAPI and ASGI Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Pydantic and Settings
pydantic[email]>=2.5.3
pydantic-settings>=2.1.0

# SQLAlchemy and Database
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.1

# Security and Encryption
python-jose[cryptography]>=3.3.0
passlib>=1.7.4
cryptography>=42.0.0
bcrypt==4.0.1

# Rate Limiting
slowapi>=0.1.9

# Supabase Client (use latest compatible version)
supabase>=2.0.0

# Caching
cachetools>=5.3.2

# Utilities
python-dotenv>=1.0.0
python-dateutil>=2.8.2

# Fuzzy Matching (for bank reconciliation)
python-Levenshtein>=0.25.1
thefuzz>=0.22.1

# PDF Generation
weasyprint>=60.0
Jinja2>=3.1.3

# Email Service
aiosmtplib>=3.0.0
email-validator>=2.1.0

# LlamaParse - optional, install separately if needed
# llama-cloud-services>=0.6.0
